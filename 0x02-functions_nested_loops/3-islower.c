#include "main.h"

/**
 * _islower - checks for lowrcase
 *
 * Returns: 1 (success)
 * Return: 0 (otherwise)
 * c - ksksa
 */
int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
		return (0);
	}
}

