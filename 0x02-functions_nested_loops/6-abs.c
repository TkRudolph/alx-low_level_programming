#include "main.h"

/**
 * _abs - computes absolute value of an interger
 *
 * @n: number to be tested
 * Return: n or -n
 */
int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
